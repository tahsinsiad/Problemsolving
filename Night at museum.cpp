#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    int cnt=0,cnt1=0;
    cin>>s;
    for(int i=0;i<s.size();i++)
    {
        if(s[i]>='a' && s[i] <='z')
        {
            if(s[i]!=)
        }
    }
}
